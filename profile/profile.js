const editBtn = document.getElementById("editBtn");
const editMessage = document.getElementById("editMessage");

editBtn.addEventListener("click", function() {
  editMessage.textContent = "Profile editing will open soon ♡";
});